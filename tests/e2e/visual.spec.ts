import { expect, test } from '@playwright/test'test.describe('Visual Regression Tests', () => {  test.describe.configure({ mode: 'serial' })  test('Login page visual', async ({ page }) => {    await page.goto('/login')    // ä½¿ç”¨ domcontentloaded ?¿å? Firebase ?Œæ™¯è«‹æ?å°Žè‡´è¶…æ?    await page.waitForLoadState('domcontentloaded')    // ç­‰å??é¢?œéµ?ƒç??ºç¾    await page.waitForTimeout(1000)    // ?ªå??»å…¥?é¢?ªå?ä¸¦è? baseline æ¯”å?    await expect(page).toHaveScreenshot('login-page.png', {      fullPage: true    })  })  test('Home page visual (unauthenticated)', async ({ page }) => {    await page.goto('/')    await page.waitForLoadState('domcontentloaded')    await page.waitForTimeout(1000)    // ?ªç™»?¥æ??å??‘åˆ° loginï¼Œæˆª?–è©²?€??    await expect(page).toHaveScreenshot('home-redirect.png', {      fullPage: true    })  })  test('Login form elements', async ({ page }) => {    await page.goto('/login')    await page.waitForLoadState('domcontentloaded')    await page.waitForTimeout(1000)    // ?å??¹å??ƒç??ªå? (è¡¨å–®?€??    const form = page.locator('form')    if ((await form.count()) > 0) {      await expect(form.first()).toHaveScreenshot('login-form.png')    }  })})
